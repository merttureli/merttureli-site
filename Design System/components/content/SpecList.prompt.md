Technical key/value rows — the spec-sheet device (`W : 292 mm : 827 px`) that carries all factual metadata: camera bodies, aircraft types, project credits.

```jsx
<SpecList items={[
  { label: "Aircraft", value: "Cessna 172S", aside: "PA-28 rating" },
  { label: "Hours", value: "412", aside: "PIC 260" },
]} />
```

`dense` for inline sidebars; hairline rows for standalone blocks.
