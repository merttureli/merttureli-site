A single portfolio entry — numeral, image, title, discipline. Borderless and shadowless: the image edge is the card edge.

```jsx
<WorkCard index="04" title="Wing rib topology" discipline="Engineering" year="2025" src="/w4.jpg" />
```

Lay them out in a grid with `gap: var(--space-8)`. Portrait `4 / 5` is the default; alternate ratios across a grid for rhythm.
