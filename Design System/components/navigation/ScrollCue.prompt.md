The scroll affordance — a hard-cornered 72px square pinned to the bottom-right of a hero; the arrow eases down 3px on hover.

```jsx
<ScrollCue inverse onClick={() => scrollTo(1)} />
<ScrollCue shape="bare" label="Scroll" />
```

It is the one square-cornered element in a system of pills and soft radii — keep it that way.
