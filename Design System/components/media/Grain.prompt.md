Film-grain overlay — the LUX signature texture. Sits inside any `position: relative` container.

```jsx
<div style={{ position: "relative" }}>
  <img src="..." alt="" />
  <Grain blend="overlay" />
</div>
```

Grain goes on photography, dark plates and grey gradient fields. Never on plain white paper.
