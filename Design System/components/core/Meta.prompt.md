Tiny letter-spaced mono caps — the system's technical voice for eyebrows, dates, spec keys, image credits and numeric indices.

```jsx
<Meta index="01">Reykjavík / 2025</Meta>
<Meta wide tone="strong">Selected work</Meta>
```

Never larger than 12px. Always uppercase. Pair with `Rule` for section headers.
