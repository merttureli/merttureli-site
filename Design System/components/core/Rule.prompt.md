Hairline divider with optional end labels — the primary structural device; the system uses rules where other systems would use boxes.

```jsx
<Rule label="Selected work" right="012" />
<Rule strong />
```

Default is a 12%-ink hairline. `strong` is reserved for the top of a major section.
